import React from 'react'
import { Text } from 'react-native'

const Header = props => <Text style={{fontSize: '200%'}} {...props} />
const Header2 = props => <Text style={{fontSize: '150%'}} {...props} />
const Header3 = props => <Text style={{fontSize: '110%'}} {...props} />

export { Header }
export { Header2 }
export { Header3 }
